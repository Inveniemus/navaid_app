import 'package:flutter/material.dart';

class AppColors {
  // Palette de base
  static const Color white = Colors.white;
  static const Color orange = Colors.orange;

  // Instruments
  static const Color instrumentBackground = Color(0xFF111111);
  static const Color instrumentRing = Color(0xFF1A1A1A);
  static const Color instrumentGrey = Color(0xFF555555);
  static const Color adfNeedle = Color(0xFFFFD700);

  // Carte / Jeu
  static const Color mapBackground = Color(0xFF257a09);
  static const Color beacon = Colors.blueAccent;
  static const Color trail = Colors.deepOrangeAccent;

  // Interface (Overlay)
  static const Color panelBackground = Colors.grey;
  static const Color panelBorder = Color(0xFF424242); // grey[800]
  static const Color controlActive = Colors.cyanAccent;
  static const Color controlInactive = Color(0xFF424242); // grey[800]
}